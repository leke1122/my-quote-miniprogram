<template name="listPage">
  <view class="container">
    <view class="list-header" wx:if="{{countLabel}}">
      <text class="muted">{{countLabel}}</text>
    </view>
    <block wx:if="{{empty}}">
      <view class="empty-box">
        <text class="empty-title">暂无数据</text>
        <text class="empty-desc">{{emptyHint}}</text>
        <button class="btn-primary" bindtap="goSettings">去设置拉取数据</button>
      </view>
    </block>
    <block wx:else>
      <view class="card list-row" wx:for="{{items}}" wx:key="id">
        <view class="list-row-main">
          <text class="list-title">{{item.title}}</text>
          <text class="list-subtitle">{{item.subtitle}}</text>
        </view>
        <text class="list-meta" wx:if="{{item.meta}}">{{item.meta}}</text>
      </view>
    </block>
  </view>
</template>
